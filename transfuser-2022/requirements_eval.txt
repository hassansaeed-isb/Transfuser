# Python dependencies for Longest6 evaluation on RTX 50-series GPUs.
#
# IMPORTANT:
#   - mmcv/mmdet are intentionally OMITTED. Evaluation uses model_eval.py (no mmcv),
#     which is what makes this work on RTX 5090 (sm_120).
#   - RTX 50-series (Blackwell / sm_120) REQUIRES a CUDA 12.8 PyTorch build (torch >= 2.7).
#     cu124/cu126 wheels lack sm_120 kernels and fail at runtime. Install torch separately:
#       pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   - Recommended Python: 3.10 (3.11 also works). Avoid 3.12 (distutils removed).

# ---- Deep learning (torch/torchvision installed separately, listed for reference) ----
timm>=0.9.0

# ---- Core scientific stack ----
# numpy pinned <2.0: old CARLA/scenario_runner code predates the numpy 2.0 API changes.
numpy>=1.24,<2.0
scipy>=1.10.0
scikit-image>=0.21.0
matplotlib>=3.7.0
Shapely>=2.0.0

# ---- Model / agent runtime ----
opencv-python>=4.8.0
Pillow>=10.0.0
tqdm>=4.65.0
ujson>=5.4.0

# ---- CARLA leaderboard + scenario_runner ----
# py-trees MUST be 0.8.3: newer 2.x has an incompatible API and breaks scenario evaluation.
py-trees==0.8.3
networkx>=2.2
xmlschema>=1.0.18
psutil>=5.9.0
six>=1.16.0
ephem>=4.1.3
requests>=2.28.0
pygame>=2.1.2
tabulate>=0.9.0
dictor>=0.1.10
PyYAML>=6.0
